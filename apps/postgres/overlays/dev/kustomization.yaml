apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: postgres
resources:
  - ../../base
# Nothing to patch yet — no hostname (nothing HTTP here), TLS is the
# Gateway's problem and Postgres isn't behind it. Dev-sizing overrides
# would land here.

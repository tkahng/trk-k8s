# cert-manager — certificate machinery (Issuers, Certificates, ACME).
# Issuers live in cluster/addons/cert-manager/issuers/ (DNS-01 via Route53;
# no inbound access needed, so the locked-down firewall stays locked).
crds:
  enabled: true   # install/upgrade the CRDs with the chart
